# 移行手順書

新しい申込フォームへの切り替え手順です。**現在このブランチはマージされておらず、
稼働中のシステムには一切影響していません。**

---

## いまの状態

| | 稼働中（`main`） | 準備中（このブランチ） |
|---|---|---|
| フォーム | 入力欄をHTMLに直接記述 | `config.json` から動的生成 |
| `apply/config.json` | v1（`features` / `standardFields` / `pricing.options` はオブジェクト） | v2（`fields[]` / `pricing.options[]`） |
| バックエンド | Apps Script（1ファイル） | `gas/`（機能ごとに分割） |
| 管理画面 | 2つに分裂・主催者には渡せない | 1画面・パスワードのみで使える |

GitHub Pages が配信しているのは `main` なので、出展者が開くフォームも
稼働中の Apps Script も従来どおり動いています。

---

## ⚠️ 先にマージしないでください

フォーム・`config.json`・Apps Script の3つは**同時にしか切り替えられません**。

- 新しいフォームは `answers` / `selectedOptions` という形で送ります。
  稼働中の Apps Script はこの形を解釈できません。
- v2 の `config.json` を稼働中の Apps Script に読ませると、
  `pricing.options` が配列になったことで**オプションと割引が黙って全部落ちます**。

  | | 旧GAS × v2設定 | 正しい金額 |
  |---|---|---|
  | 壁側半テーブル＋コンセント＋懇親会2名＋会員割引 | **¥9,000** | **¥17,500** |

- さらに v2 では `spreadsheetId` を公開ファイルから外したため、
  稼働中の Apps Script は保存先を見失って例外になります。

**マージ＝切り替えの実行**だと考えてください。部分マージや混在運用はできません。

---

## 第0段階：Apps Script を用意する前に画面を見る（デモ）

申込フォームはローカルサーバで開けば動きますが、**管理画面は Apps Script が無いと
ログインすらできません**（設定の取得も保存もサーバ経由のため）。
そこで、通信部分だけをブラウザ内で模擬したデモを生成できるようにしてあります。

```bash
node tools/build-demo.js        # demo/index.html を生成
```

生成された `demo/index.html` をブラウザで開くと、申込フォームと管理画面を
タブで切り替えて触れます。ファイル1つで完結しているので、スマホに送って開くこともできます。

- 管理パスワードは `demo`（入力済み）、リピーター認証の確認コードは `1234`
- 管理画面で保存すると、その場で申込フォームに反映されます
- **料金計算・必須チェック・ブース別のオプション可否は本番と同じコードが動きます**
  （`apply/config-schema.js` をそのまま読み込んでいるため、金額は本番と一致します）

模擬しているのは通信の外側だけです。次のものは**デモでは確認できません**。
第1段階以降で初めて確認できます。

- メール送信、スプレッドシートへの保存、Google ドライブへの画像保存
- GitHub への設定の書き込み（保存はメモリ上だけ。開き直すと元に戻ります）
- LINE 連携（LIFF）

生成物の `demo/` は `.gitignore` に入れてあります。
本物と見分けのつく必要があるため、**GitHub Pages では配信しません**。
デモのメール文面では、振込先の数字を伏せてあります。

---

## 第1段階：本番に触れずに準備・検証する

要点は **`CONFIG_JSON_URL` をこのブランチの raw URL に向けること**です。
そうすると新しい Apps Script は `main` の本番設定を一切見ないので、
本番と完全に分離したまま構築・検証できます。

### 1. 新しい Apps Script プロジェクトを作る

**主催者の Google アカウント**で新規作成します。稼働中のプロジェクトは触りません。

> Apps Script のウェブアプリは「デプロイした人として実行」されます。
> 主催者アカウントでデプロイすることで、作られるスプレッドシート・フォルダ・
> 送信メールがすべて主催者の所有になります。

`gas/` の各ファイルを同じ名前で貼り付けてください（`appsscript.json` はマニフェスト）。

### 2. スクリプトプロパティを設定する

プロジェクトの設定 → スクリプト プロパティ

| キー | 値 |
|---|---|
| `CONFIG_JSON_URL` | 下記の**ブランチのURL**（本番ではない） |
| `ADMIN_PASSWORD` | 管理画面のパスワード（任意に決める） |
| `DRIVE_ROOT_FOLDER_ID` | 画像を保存する親フォルダ。**URLのままでも可** |
| `ADMIN_EMAIL` | 日次ダイジェストの宛先 |
| `GITHUB_TOKEN` | 管理画面から設定を保存するためのトークン |
| `GITHUB_REPO` | `bayashichan/buchiiyashifestaodawara` |
| `GITHUB_BRANCH` | `claude/application-form-system-voc2ur` |

検証中の `CONFIG_JSON_URL`:

```
https://raw.githubusercontent.com/bayashichan/buchiiyashifestaodawara/claude/application-form-system-voc2ur/apply/config.json
```

`GITHUB_TOKEN` は fine-grained personal access token で、
**このリポジトリのみ・Contents の Read and write** に絞ってください。

### 3. ウェブアプリとしてデプロイする

デプロイ → 新しいデプロイ → 種類「ウェブアプリ」

- **次のユーザーとして実行**: 自分
- **アクセスできるユーザー**: 全員

初回実行時に Drive・Gmail・スプレッドシートの権限を承認します。
発行された `/exec` の URL を控えておきます。

### 4. 設定を確認する

GASエディタで `checkSetup()` を実行します。設定・権限・接続の状態が一覧で出ます。
最後に「申込を受け付けられる状態です」と出れば次へ進めます。
❌ が付いた項目を先に直してください。

### 5. フル機能テストを実行する

GASエディタで **`runFullTest()`** を実行します。**1回の実行で全機能を通しで確認します。**

| 確認する内容 | 実際に起きること |
|---|---|
| 本番から切り離されているか | `GITHUB_BRANCH` が `main` なら**中止する** |
| 設定の取得と点検 | ブランチの config.json を読み、致命的な誤りが無いか見る |
| スプレッドシート・フォルダの作成 | 主催者のドライブに実際に作られる |
| 画像の保存 | テスト画像が実際に Drive へ保存される |
| 申込の受付と再計算 | 金額がサーバー側の計算と一致するか照合する |
| スプレッドシートへの転記 | 行が増え、合計金額と画像URLが正しい列に入るか |
| マスターDBへの転記 | 2つ目のシートにも入るか |
| 確認メールの送信 | **実際に届く**（宛先は実行した本人） |
| メール文面の差し込み | `{{...}}` が残っていないか、`¥¥` になっていないか |
| リピーター照合 | 認証コードの発行 → 検証 → 過去申込の読み出し |

最後に「判定: すべて通りました」と出れば、GAS 側は本番と同じ動きをしています。
❌ が出た項目は、その行に理由が書かれています。

補助的に使う関数:

| 関数 | 用途 |
|---|---|
| `previewConfirmationMail()` | メール本文を**送らずに**確認する |
| `testSubmission()` | 申込を1件だけ流す（`runFullTest` の一部） |
| `cleanupTestData()` | テストで入った行を消す（**目印のある行だけ**。本物の申込は消さない） |
| `removeTriggers()` | 検証用プロジェクトの自動実行を止める |
| `runMailQueueNow()` | 送信枠が尽きて溜まったメールを流す |

> `runFullTest()` はテスト行に目印（`★自動テスト★`）を付けて書き込みます。
> `cleanupTestData()` はこの目印のある行だけを消すので、本物の申込は残ります。
> Drive に保存されたテスト画像は消えないので、フォルダから手で削除してください。

### 6. 管理画面とフォームを確認する

管理画面（`admin/index.html`）を**ローカルで**開き、パスワードでログインします。

```bash
python3 -m http.server 8000
# → http://localhost:8000/admin/index.html
# → http://localhost:8000/apply/index.html
```

管理画面から設定を保存すると、`GITHUB_BRANCH` に指定したブランチの
`apply/config.json` が書き換わります。**`main` ではないことを必ず確認してください。**

GitHub Pages は `main` を配信し続けるので、**出展者が開くURLは変わりません。**

---

## 検証中に本番を壊さないための守り

検証用プロジェクトが本番に触れてしまう経路は2つあり、どちらもコード側で塞いであります。

**1. 設定の保存先ブランチ**

`GITHUB_BRANCH` に既定値はありません。未設定のまま管理画面で保存を押すと、
書き込まずにエラーを返します。既定を `main` にしていると、検証用プロジェクトで
保存を押しただけで稼働中のフォームの設定が変わってしまうためです。

**2. 同名スプレッドシートの再利用**

「新規開催回を始める」は同名のスプレッドシートがあれば再利用しますが、
**自分が所有するものだけ**を対象にします。旧スプレッドシートを閲覧用に
共有されている状態で名前が一致すると、テスト行が稼働中のデータに
入ってしまうためです。所有者を確認できない場合も再利用せず、新規に作ります。

**3. 検証用プロジェクトで絶対に設定してはいけない値**

| キー | 理由 |
|---|---|
| `CURRENT_SPREADSHEET_ID` / `DATABASE_SPREADSHEET_ID` に本番のID | テスト行が本番のシートに入ります。空にしておけば自動で新規作成されます |
| `DRIVE_ROOT_FOLDER_ID` に本番の画像フォルダ | テスト画像が本番のフォルダに混ざります |
| `GITHUB_BRANCH` に `main` | 管理画面の保存で本番の設定が変わります |

検証が終わったら `removeTriggers()` で自動実行を止めておくと、
検証用プロジェクトから日次ダイジェストが届き続けるのを防げます。

---

## 第2段階：切り替え当日

準備がすべて済んでから実施します。**申込の谷間（深夜など）を選んでください。**

- [ ] 1. `CONFIG_JSON_URL` を**本番のURL**に変更する
      `https://bayashichan.github.io/buchiiyashifestaodawara/apply/config.json`
- [ ] 2. `GITHUB_BRANCH` を `main` に変更する
- [ ] 3. `apply/config.json` の `integration.gasUrl` を**新しい `/exec` URL** にして
      ブランチへ push する
- [ ] 4. **PR をマージする**
- [ ] 5. GitHub Pages の反映を待つ（数分）
- [ ] 6. 本番URLでテスト申込を1件送る
- [ ] 7. 確認する
      - [ ] 金額がフォームの表示どおりか
      - [ ] スプレッドシートに行が入ったか
      - [ ] Drive に画像が保存されたか
      - [ ] 確認メールが届き、**差出人が主催者のアドレス**か
      - [ ] メール本文に `{{` が残っていないか
- [ ] 8. テスト行をスプレッドシートから削除する
- [ ] 9. 稼働していた旧プロジェクトの**デプロイを停止**する（削除はしない）

### 過去データの移送（任意・切り替え後でよい）

現在の申込スプレッドシートは**スタッフ個人の Google アカウント所有**です。
過去の申込をリピーター機能で引けるようにするには、マスターDBへ移送します。

- [ ] スタッフに、そのスプレッドシートの**閲覧権限を主催者アカウントへ**付与してもらう
- [ ] 管理画面 → 開催回のセットアップ → 移送元のIDを入力
- [ ] 「対応表を確認」でどの列がどの項目に入るか下見する
- [ ] 問題なければ「移送を実行」

移送元は**読み取るだけで変更しません**。同じ申込を二重に取り込むこともありません。

---

## 切り戻し

問題が起きたら、マージを丸ごと戻すのが最も確実です。

```bash
git revert -m 1 <マージコミット>
git push
```

これで `apply/config.json` も v1 に戻り、稼働中だった Apps Script が
そのまま動きます。**旧プロジェクトを削除しないのはこのためです。**

部分的に戻す場合は、`apply/config.json` を v1 に戻したうえで
`integration.gasUrl` を旧の `/exec` URL に戻してください。
どちらか片方だけ戻すと動きません。

---

## 切り替えで何が変わるか

### よくなること

- **金額の改ざんができなくなる。** 旧は早割の適用可否をブラウザからの申告
  （`isEarlyBird`）をそのまま信じており、書き換えれば早割価格で申し込めました。
  新はサーバ側で締切から判定します。ブースごとのオプション上限も検証します
  （旧は電源の可否しか見ておらず、スタッフ99名でも通りました）。
- **メールが送れなくても申込を失わない。** 旧は保存後にメール送信で失敗すると
  エラーを返すため、**保存済みなのに利用者にはエラーが出て、再送信して二重申込**
  になっていました。新は送信待ちに積んで受付は成立させます。
- **メール送信枠の消費が半分以下になる。** 管理者への通知を1件ごとではなく
  1日1通のダイジェストにまとめます（無料Gmailは1日100宛先まで）。
- **過去申込の呼び出しに使う認証コードが総当たりされなくなる。** 旧は10分間、
  何度でも試せました。新は5回誤ると無効化し、再送も60秒に1回に制限します
  （4桁＝1万通りなので、ここは厳しめにしています。管理画面のログインとは別の仕組みです）。
- 項目・オプション・ブースを管理画面から自由に設定できる。
- 開催回をまたいだ過去申込の照合ができる（マスターDB）。
- 項目名を変更しても過去データの照合が壊れない（`_meta` の項目ID対応）。

### 変わること（運用に影響しうる点）

- **管理者への申込通知が即時ではなくなります。** 1日1通のダイジェストになります。
  すぐ知りたい場合は管理画面の「今すぐダイジェストを送る」を使ってください。
- **保存先スプレッドシートが変わります。** 新しく作られたものに入るため、
  切り替え前後で見る場所が変わります。
- **画像URLの形式が変わります。** 旧は `drive.google.com/file/d/.../view`、
  新は `lh3.googleusercontent.com/d/...`（埋め込み向きの直リンク）。
- **出展者一覧API（`get_exhibitors`）は移植していません。** 使っていないことを
  確認済みですが、もし外部から叩いている箇所があれば実装が必要です。

---

## 管理画面へのアクセスについて

**管理画面のURLは、知っていれば誰でも開けます。** GitHub Pages はリポジトリ内の
ファイルをすべて公開配信するためで、これは現行の管理画面（`quick-settings.html` /
`config-editor.html`）も同じです。

守りはパスワードに集約しています。

- 開いても出るのはログイン画面だけです。設定内容・スプレッドシートのID・
  メール残量はすべて Apps Script 側にあり、パスワードなしでは取得できません
- パスワードを10回続けて間違えると60秒間ロックされます
- 検索エンジンには出ないようにしてあります（`noindex`）

現行より改善している点として、**GitHubのトークンをブラウザに置かなくなりました。**
現在の `quick-settings.html` はトークンを画面に入力させて端末に保存する方式で、
これが漏れるとリポジトリを書き換えられます。新しい方はトークンを Apps Script 側に
置くので、ブラウザには一切出ません。

### 運用でお願いしたいこと

- **パスワードは推測されにくいものにしてください。** 守りはここだけです
- 「この端末にパスワードを保存する」は**自分の端末でだけ**チェックしてください。
  共用のパソコンでは外してください
- 管理画面のURLを、出展者向けの案内やSNSに載せないでください

> **補足（技術的な注意）**: ログイン失敗の回数は全体で1つのカウンタで数えています。
> そのため、第三者が誤ったパスワードを送り続けると、正規の利用者もロックされます。
> 主催者から「パスワードは合っているのに入れない」と言われたら、まずこれを疑って
> ください。1分待てば解除されます。

## 移行前に直したい設定

管理画面を開くと画面上部に警告として出ます。

**早割価格が通常価格より高いブースが2件あります。**

| ブース | 通常価格 | 早割価格 |
|---|---|---|
| 内側半テーブル | ¥8,000 | **¥9,000** |
| 壁側半テーブル | ¥9,000 | **¥17,000** |

早割が無効な現在は実害がありませんが、**有効にした瞬間に値上げになります。**
入力ミスと思われるので、切り替えの前後で見直してください。

---

## 困ったときは

| 症状 | 確認すること |
|---|---|
| 申込が失敗する | GASエディタで `checkSetup()` を実行。❌ の項目を直す |
| メールに `{{` が残る | `previewConfirmationMail()` で未展開の差し込みを確認 |
| 金額が合わない | 管理画面のブース・オプション設定と、可否のマトリクス表を確認 |
| メールが届かない | 管理画面右上の残量表示を確認。0なら翌日に自動送信される |
| 設定を変えても反映されない | 設定の再読み込みは最大60秒かかる。それ以上なら `checkSetup()` |

開発者向けの詳細は `README.md` を参照してください。
